{
  "$schema": "https://opencode.ai/config.json",
  "plugin": ["opencode-gemini-auth@latest"],
  "agent": {
    "command-runner": {
      "description": "Executes shell commands and terminal operations",
      "mode": "primary",
      "prompt": "You are a command execution specialist. Execute the requested shell commands efficiently and safely. Always explain what the command does before running it. Report the output clearly and handle errors appropriately.",
      "tools": {
        "bash": true,
        "read": true
      }
    }
  }
}
